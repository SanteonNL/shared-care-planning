### Initiate a new exchange, returns a user redirect URL and exchange ID
POST http://localhost:8080/api/start-exchange
Content-Type: application/json

{
  "patient": {
    "system": "http://fhir.nl/fhir/NamingSystem/bsn",
    "value": "999910413"
  }
}

> {%
    client.assert(response.status === 200, "Response status is not 200");
    console.log(response.body.redirect_uri);
    client.global.set("exchange_id", response.body.exchange_id);
%}

### Get the status of an exchange, returns the data when available
GET http://localhost:8080/api/exchange/{{exchange_id}}

